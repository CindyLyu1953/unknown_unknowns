# Used only by the local generation script. Never expose this value to Vite.
OPENAI_API_KEY=

# Optional. The script defaults to a cost-sensitive model suitable for batch generation.
OPENAI_MODEL=gpt-5.6-luna
